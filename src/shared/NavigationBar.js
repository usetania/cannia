import React from 'react';
import Navbar from 'react-bootstrap/Navbar';

const NavigationBar = () => (
  <Navbar bg="light">
    <Navbar.Brand href="/">
      {'Plant Finder'}
    </Navbar.Brand>
  </Navbar>
);

export default NavigationBar;
